Coloca aquí el archivo `logo-metalicas-hernandez.png` (la foto real del letrero de la empresa).

El componente `CompanyHeader` lo busca en `assets/logo-metalicas-hernandez.png`. Si el archivo no existe, se muestra automáticamente un monograma "MH" en SVG como respaldo, así que la app funciona igual mientras tanto.
